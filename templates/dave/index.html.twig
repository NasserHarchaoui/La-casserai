{% extends 'base.html.twig' %}

{% block title %}Dave index{% endblock %}

{% block body %}
    <h1>Dave index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Leeftijd</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for dave in daves %}
            <tr>
                <td>{{ dave.id }}</td>
                <td>{{ dave.leeftijd }}</td>
                <td>
                    <a href="{{ path('dave_show', {'id': dave.id}) }}">show</a>
                    <a href="{{ path('dave_edit', {'id': dave.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('dave_new') }}">Create new</a>
{% endblock %}
